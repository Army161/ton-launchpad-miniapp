#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; Launchpad_LaunchpadFactory.headers.fc
;;
;; Header files for LaunchpadFactory
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; $JettonSetup$_store
builder $JettonSetup$_store(builder build_0, (int, slice, slice, cell, int, int) v) inline;

;; $JettonSetup$_store_cell
cell $JettonSetup$_store_cell((int, slice, slice, cell, int, int) v, builder b) inline;

;; $JettonSetup$_load_bounced
(slice, ((int))) $JettonSetup$_load_bounced(slice sc_0) inline;

;; $LaunchpadFactory$_store
builder $LaunchpadFactory$_store(builder build_0, (slice, slice, slice, int) v) inline;

;; $LaunchpadFactory$_load
(slice, ((slice, slice, slice, int))) $LaunchpadFactory$_load(slice sc_0) inline;

;; $FactoryInfo$_to_external
(slice, slice, slice, int, int, int, int, int) $FactoryInfo$_to_external(((slice, slice, slice, int, int, int, int, int)) v) inline;

;; $LaunchpadJetton$init$_store
builder $LaunchpadJetton$init$_store(builder build_0, (slice, slice, int) v) inline;

;; $LaunchpadFactory$init$_load
(slice, ((slice, slice, slice))) $LaunchpadFactory$init$_load(slice sc_0) inline;

;; $LaunchpadFactory$_contract_init
(slice, slice, slice, int) $LaunchpadFactory$_contract_init(slice $owner, slice $treasury, slice $liquidityManager) impure inline;

;; $LaunchpadFactory$_contract_load
(slice, slice, slice, int) $LaunchpadFactory$_contract_load() impure inline;

;; $LaunchpadFactory$_contract_store
() $LaunchpadFactory$_contract_store((slice, slice, slice, int) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $global_contractAddress
slice $global_contractAddress((cell, cell) $s) impure inline;

;; $LaunchpadJetton$_init_child
(cell, cell) $LaunchpadJetton$_init_child(slice $factory, slice $creator, int $salt) inline_ref;

;; $SendParameters$_constructor_to_value_mode_bounce_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline;

;; $SendParameters$_constructor_to_value_mode_bounce_body_code_data
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body_code_data(slice $to, int $value, int $mode, int $bounce, cell $body, cell $code, cell $data) inline;

;; $FactoryInfo$_constructor_owner_treasury_liquidityManager_launchCount_launchFee_graduationTarget_tradeFeeBps_creatorFeeBps
((slice, slice, slice, int, int, int, int, int)) $FactoryInfo$_constructor_owner_treasury_liquidityManager_launchCount_launchFee_graduationTarget_tradeFeeBps_creatorFeeBps(slice $owner, slice $treasury, slice $liquidityManager, int $launchCount, int $launchFee, int $graduationTarget, int $tradeFeeBps, int $creatorFeeBps) inline;

;; $LaunchpadFactory$_fun_get_factory_info
((slice, slice, slice, int), (slice, slice, slice, int, int, int, int, int)) $LaunchpadFactory$_fun_get_factory_info((slice, slice, slice, int) $self) impure inline_ref;

;; $LaunchpadFactory$_fun_get_jetton_address
((slice, slice, slice, int), slice) $LaunchpadFactory$_fun_get_jetton_address((slice, slice, slice, int) $self, slice $creator, int $salt) impure inline_ref;

;; $JettonSetup$_constructor_queryId_treasury_liquidityManager_content_launchFee_initialBuyTon
((int, slice, slice, cell, int, int)) $JettonSetup$_constructor_queryId_treasury_liquidityManager_content_launchFee_initialBuyTon(int $queryId, slice $treasury, slice $liquidityManager, cell $content, int $launchFee, int $initialBuyTon) inline;


;; Launchpad_LaunchpadFactory.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_contractAddress((cell, cell) $s) impure inline {
    var (($s'code, $s'data)) = $s;
    return $global_contractAddressExt(0, $s'code, $s'data);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

;; Launchpad_LaunchpadFactory.constants.fc
;; Cell GCiJbedRErzqowEqwxKmfnTd4iwuOmaY8jnHu9aJVKM=
cell __gen_cell_cell_1828896de75112bceaa3012ac312a67e74dde22c2e3a6698f239c7bbd68954a3() asm """
    B{b5ee9c7241010101001600002800000000466163746f72792077697468647261777a72fa08} B>boc PUSHREF
""";

;; Launchpad_LaunchpadFactory.storage.fc
;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, $bounce);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body_code_data(slice $to, int $value, int $mode, int $bounce, cell $body, cell $code, cell $data) inline {
    return ($mode, $body, $code, $data, $value, $to, $bounce);
}

;;
;; Type: JettonSetup
;; Header: 0x4c500002
;; TLB: jetton_setup#4c500002 queryId:uint64 treasury:address liquidityManager:address content:^cell launchFee:coins initialBuyTon:coins = JettonSetup
;;

builder $JettonSetup$_store(builder build_0, (int, slice, slice, cell, int, int) v) inline {
    var (v'queryId, v'treasury, v'liquidityManager, v'content, v'launchFee, v'initialBuyTon) = v;
    build_0 = store_uint(build_0, 1280311298, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    build_0 = build_0.store_slice(v'treasury);
    build_0 = build_0.store_slice(v'liquidityManager);
    build_0 = build_0.store_ref(v'content);
    build_0 = build_0.store_varuint16(v'launchFee);
    build_0 = build_0.store_varuint16(v'initialBuyTon);
    return build_0;
}

cell $JettonSetup$_store_cell((int, slice, slice, cell, int, int) v, builder b) inline {
    return $JettonSetup$_store(b, v).end_cell();
}

(slice, ((int))) $JettonSetup$_load_bounced(slice sc_0) inline {
    var v'queryId = sc_0~load_uint(64);
    return (sc_0, (v'queryId));
}

((int, slice, slice, cell, int, int)) $JettonSetup$_constructor_queryId_treasury_liquidityManager_content_launchFee_initialBuyTon(int $queryId, slice $treasury, slice $liquidityManager, cell $content, int $launchFee, int $initialBuyTon) inline {
    return ($queryId, $treasury, $liquidityManager, $content, $launchFee, $initialBuyTon);
}

;;
;; Type: FactoryInfo
;; TLB: _ owner:address treasury:address liquidityManager:address launchCount:uint32 launchFee:coins graduationTarget:coins tradeFeeBps:int257 creatorFeeBps:int257 = FactoryInfo
;;

(slice, slice, slice, int, int, int, int, int) $FactoryInfo$_to_external(((slice, slice, slice, int, int, int, int, int)) v) inline {
    var (v'owner, v'treasury, v'liquidityManager, v'launchCount, v'launchFee, v'graduationTarget, v'tradeFeeBps, v'creatorFeeBps) = v; 
    return (v'owner, v'treasury, v'liquidityManager, v'launchCount, v'launchFee, v'graduationTarget, v'tradeFeeBps, v'creatorFeeBps);
}

((slice, slice, slice, int, int, int, int, int)) $FactoryInfo$_constructor_owner_treasury_liquidityManager_launchCount_launchFee_graduationTarget_tradeFeeBps_creatorFeeBps(slice $owner, slice $treasury, slice $liquidityManager, int $launchCount, int $launchFee, int $graduationTarget, int $tradeFeeBps, int $creatorFeeBps) inline {
    return ($owner, $treasury, $liquidityManager, $launchCount, $launchFee, $graduationTarget, $tradeFeeBps, $creatorFeeBps);
}

;;
;; Type: LaunchpadJetton
;; TLB: _ factory:address creator:address salt:uint64 initialized:bool treasury:address liquidityManager:address content:^cell totalSupply:coins virtualTon:coins virtualTokens:coins realTonRaised:coins graduated:bool migrated:bool totalCreatorFees:coins totalPlatformFees:coins tradeCount:uint32 = LaunchpadJetton
;;

builder $LaunchpadJetton$init$_store(builder build_0, (slice, slice, int) v) inline {
    var (v'factory, v'creator, v'salt) = v;
    build_0 = build_0.store_slice(v'factory);
    build_0 = build_0.store_slice(v'creator);
    build_0 = build_0.store_int(v'salt, 257);
    return build_0;
}

cell $LaunchpadJetton$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $LaunchpadJetton$_init_child(slice $factory, slice $creator, int $salt) inline_ref {
    ;; Build init code cell

    ;; Contract Code: LaunchpadJetton
    cell init_code = $LaunchpadJetton$_child_get_code();


    builder b = begin_cell();
    b = b.store_int(false, 1);
    b = $LaunchpadJetton$init$_store(b, ($factory, $creator, $salt));
    return (init_code, b.end_cell());
}

;;
;; Type: LaunchpadFactory
;; TLB: _ owner:address treasury:address liquidityManager:address launchCount:uint32 = LaunchpadFactory
;;

builder $LaunchpadFactory$_store(builder build_0, (slice, slice, slice, int) v) inline {
    var (v'owner, v'treasury, v'liquidityManager, v'launchCount) = v;
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_slice(v'treasury);
    build_0 = build_0.store_slice(v'liquidityManager);
    build_0 = build_0.store_uint(v'launchCount, 32);
    return build_0;
}

(slice, ((slice, slice, slice, int))) $LaunchpadFactory$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'treasury = sc_0~load_msg_addr();
    var v'liquidityManager = sc_0~load_msg_addr();
    var v'launchCount = sc_0~load_uint(32);
    return (sc_0, (v'owner, v'treasury, v'liquidityManager, v'launchCount));
}

(slice, ((slice, slice, slice))) $LaunchpadFactory$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'treasury = sc_0~load_msg_addr();
    var v'liquidityManager = sc_0~load_msg_addr();
    return (sc_0, (v'owner, v'treasury, v'liquidityManager));
}

(slice, slice, slice, int) $LaunchpadFactory$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$LaunchpadFactory$_load();
    }
    else {
        (slice $owner, slice $treasury, slice $liquidityManager) = $sc~$LaunchpadFactory$init$_load();
        $sc.end_parse();
        return $LaunchpadFactory$_contract_init($owner, $treasury, $liquidityManager);
    }
}

() $LaunchpadFactory$_contract_store((slice, slice, slice, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $LaunchpadFactory$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract LaunchpadFactory functions
;;

(slice, slice, slice, int) $LaunchpadFactory$_contract_init(slice $owner, slice $treasury, slice $liquidityManager) impure inline {
    var (($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount)) = (null(), null(), null(), null());
    $self'owner = $owner;
    $self'treasury = $treasury;
    $self'liquidityManager = $liquidityManager;
    $self'launchCount = 0;
    return ($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount);
}

((slice, slice, slice, int), (slice, slice, slice, int, int, int, int, int)) $LaunchpadFactory$_fun_get_factory_info((slice, slice, slice, int) $self) impure inline_ref {
    var (($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount)) = $self;
    var $fresh$ret_28 = $FactoryInfo$_constructor_owner_treasury_liquidityManager_launchCount_launchFee_graduationTarget_tradeFeeBps_creatorFeeBps($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount, 50000000, 1500000000000, 200, 6000);
    return (($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount), $fresh$ret_28);
}

((slice, slice, slice, int), slice) $LaunchpadFactory$_fun_get_jetton_address((slice, slice, slice, int) $self, slice $creator, int $salt) impure inline_ref {
    var (($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount)) = $self;
    var $fresh$ret_29 = $global_contractAddress($LaunchpadJetton$_init_child($global_myAddress(), $creator, $salt));
    return (($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount), $fresh$ret_29);
}

;;
;; Get methods of a Contract LaunchpadFactory
;;

_ %get_factory_info() method_id(112759) {
    var self = $LaunchpadFactory$_contract_load();
    var res = self~$LaunchpadFactory$_fun_get_factory_info();
    return $FactoryInfo$_to_external(res);
}

_ %get_jetton_address(slice $creator, int $salt) method_id(118368) {
    slice $creator = $creator;
    int $salt = $salt;
    var self = $LaunchpadFactory$_contract_load();
    var res = self~$LaunchpadFactory$_fun_get_jetton_address($creator, $salt);
    return res;
}

;;
;; Routing of a Contract LaunchpadFactory
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount) = $LaunchpadFactory$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) {
        ;; Skip 0xFFFFFFFF prefix of a bounced message
        in_msg~skip_bits(32);
        int op = 0;
        if (slice_bits(in_msg) >= 32) {
            op = in_msg~load_uint(32);
        }
        ;; Bounced handler for JettonSetup message
        if (op == 0x4c500002) {
            var ($msg'queryId) = in_msg~$JettonSetup$_load_bounced();
            $LaunchpadFactory$_contract_store(($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount));
            return ();
        }
        
        return ();
    }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive CreateToken message
    if (op == 0x4c500001) {
        var $msg'queryId = in_msg~load_uint(64);
        var $msg'salt = in_msg~load_uint(64);
        var $msg'content = in_msg~load_ref();
        var $msg'initialBuyTon = in_msg~load_varuint16();
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        throw_unless(10854, ( (($msg'initialBuyTon == 0)) ? (true) : (($msg'initialBuyTon >= 10000000)) ));
        throw_unless(46558, ($msg'initialBuyTon <= 100000000000));
        throw_unless(30356, ($ctx'value >= ((50000000 + $msg'initialBuyTon) + 150000000)));
        $self'launchCount = ($self'launchCount + 1);
        var ($init'code, $init'data) = $LaunchpadJetton$_init_child($global_myAddress(), $ctx'sender, $msg'salt);
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body_code_data($global_contractAddress(($init'code, $init'data)), 0, 64, true, $JettonSetup$_store_cell($JettonSetup$_constructor_queryId_treasury_liquidityManager_content_launchFee_initialBuyTon($msg'queryId, $self'treasury, $self'liquidityManager, $msg'content, 50000000, $msg'initialBuyTon), begin_cell()), $init'code, $init'data));
        $LaunchpadFactory$_contract_store(($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount));
        return ();
    }
    
    ;; Receive FactoryWithdraw message
    if (op == 0x4c500005) {
        var $msg'queryId = in_msg~load_uint(64);
        throw_unless(35499, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
        $global_nativeReserve(50000000, 0);
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'owner, 0, 128, false, __gen_cell_cell_1828896de75112bceaa3012ac312a67e74dde22c2e3a6698f239c7bbd68954a3()));
        $LaunchpadFactory$_contract_store(($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount));
        return ();
    }
    
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        $LaunchpadFactory$_contract_store(($self'owner, $self'treasury, $self'liquidityManager, $self'launchCount));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}