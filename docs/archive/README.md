# Archived notes

These files were written before the 2026-09-22 contract rewrite and describe designs, tasks or processes that no
longer match the code (for example the old bonding-curve contract, the JSON-file database and fake deploy steps).
They are kept for history only. Current documentation starts at [`CLAUDE.md`](../../CLAUDE.md) and
[`docs/STATUS.md`](../STATUS.md).
